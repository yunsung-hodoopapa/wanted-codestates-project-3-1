import React from 'react';
import styled from 'styled-components';

const RepoDetail = () => {
  return <Wrapper>메인 우측 레포 디테일 컴포넌트</Wrapper>;
};

const Wrapper = styled.nav`
  background-color: var(--light-gray);
  width: 550px;
  max-width: 570px;
  height: 800px;
`;

export default RepoDetail;
